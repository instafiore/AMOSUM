nurse(1..41).
maxHoursPerYear(1692).
minHoursPerYear(1687).

minNurseMorning(6). maxNurseMorning(9).
minNurseAfternoon(6). maxNurseAfternoon(9).
minNurseNight(4). maxNurseNight(7).

minNights(58). maxNights(61).
minDays(74). maxDays(82).
