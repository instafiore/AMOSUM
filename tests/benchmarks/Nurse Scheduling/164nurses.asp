nurse(1..164).
maxHoursPerYear(1692).
minHoursPerYear(1687).

minNurseMorning(24). maxNurseMorning(36).
minNurseAfternoon(24). maxNurseAfternoon(36).
minNurseNight(16). maxNurseNight(28).

minNights(58). maxNights(61).
minDays(74). maxDays(82).
